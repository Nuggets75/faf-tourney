{
  "name": "faf-tourney",
  "version": "1.0.0",
  "description": "Self-hosted tournament manager for FAF. Zero runtime dependencies: plain Node.js http, JSON file storage, no build step, no npm install in production.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check challonge.js && node --check lib/util.js",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["faf", "tournament", "supreme-commander"],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
