{
  "name": "faf-tourney",
  "version": "1.0.0",
  "description": "Self-hosted tournament manager for FAF. Zero runtime dependencies: plain Node.js http, JSON file storage, no build step, no npm install in production.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "for f in server.js challonge.js public/*.js lib/*.js; do node --check \"$f\" || exit 1; done",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "faf",
    "tournament",
    "supreme-commander"
  ],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "acorn": "^8.12.1",
    "typescript": "^5.6.3"
  }
}
